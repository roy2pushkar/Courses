import React from "react";

const Cards = (props) => {
  let courses = props.courses;
  console.log("Printing Data");
  console.log(courses);
  return (
    <div>
      <nav></nav>
    </div>
  );
};

export default Cards;
